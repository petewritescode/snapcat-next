describe('UploadForm', () => {
  // TODO Can't yet be tested because it relies on React/ReactDOM canary
  // features and Jest only has access to the stable version specified in
  // package.json
  it.todo('renders a form with an image input');
  it.todo('fires the server action on submission');
  it.todo('displays an error message on failure');
});
